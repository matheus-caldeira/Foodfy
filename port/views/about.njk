{% extends "layout.njk" %}

{% block head %}
  <title>Sobre - Matheus Caldeira</title>
{% endblock head %}

{% block content %}

  <div id="wrapper">
    <img src="{{data.avatar_url}}" alt="{{data.name}}">
    <h1>{{data.name}}</h1>
    <h2>{{data.role}}k</h2>
    <p>{{data.description}}</p>
  </div>

{% endblock content %}